[build]
builder = "NIXPACKS"
buildCommand = "mvn clean install -DskipTests -Dmaven.resources.skip=true"

[deploy]
startCommand = "java -jar target/*.jar"
healthcheckPath = "/"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5