# CONFIGURACION DE SERVIDOR
server.port=${PORT:8080}
# Solucion para Tracking Prevention entre Netlify y Render
server.servlet.session.cookie.same-site=none
server.servlet.session.cookie.secure=true
server.servlet.session.cookie.http-only=true

# CONFIGURACION DE BASE DE DATOS (AIVEN)
spring.datasource.url=jdbc:mysql://tmcreation-tmcreation.a.aivencloud.com:26418/defaultdb?ssl-mode=REQUIRED
spring.datasource.username=avnadmin
spring.datasource.password=AVNS_zdgEHaDy62xT3TfVTdK
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# HIBERNATE - MYSQL 8
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# CONFIGURACION DE CORREO (GMAIL SSL 465)
# Se mantiene por si el sistema usa JavaMailSender en otras partes
spring.mail.host=smtp.gmail.com
spring.mail.port=465
spring.mail.username=tmcreation233@gmail.com
spring.mail.password=ngescgizoncblnmj
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.ssl.enable=true
spring.mail.properties.mail.smtp.starttls.enable=false

# CONFIGURACION RESEND (Para EmailServiceImpl)
resend.api.key=${RESEND_API_KEY}

# SEGURIDAD Y OAUTH2 (Google Login)
spring.security.oauth2.client.registration.google.client-id=${GOOGLE_CLIENT_ID}
spring.security.oauth2.client.registration.google.client-secret=${GOOGLE_CLIENT_SECRET}

# CONFIGURACION CLOUDINARY (Imagenes)
cloudinary.cloud_name=${CLOUDINARY_CLOUD_NAME}
cloudinary.api_key=${CLOUDINARY_API_KEY}
cloudinary.api_secret=${CLOUDINARY_API_SECRET}

# CONFIGURACION DE ARCHIVOS (Subida de fotos)
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB